# Spring Boot Configuration
spring.application.name=webhook-challenge

# Logging configuration
logging.level.com.example.webhookchallenge=INFO
logging.level.org.springframework.web=INFO

# Server configuration (not needed since no controllers, but good to have)
server.port=8080

# WebClient timeout configurations
spring.webflux.timeout=30s
